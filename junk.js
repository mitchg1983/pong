  // const timer2 = [];

  // futureTime = timer + 200;

  // timer2.push(futureTime);
  // timer2.push(timer);

  // console.log(futureTime);
  // console.log(timer);

  // while (timer < timer2[0]) {
  //   let imgOp = faster.style.opacity;
  //   imgOp = imgOp + 0.05;
  //   faster.style.opacity = imgOp;
  // }

  // let imgOp = faster.dataset.opacity;

  // const tester1 = faster.getPropertyValue("opacity");

  // console.log(faster.opacity);
  // console.log(tester1);

  //   let fasterOpacity = window.getComputedStyle(faster).getPropertyValue("opacity");

  //   let newOpac = 0.9;

  //   faster.style.opacity = newOpac;

  // console.log(CSSprop);

      // timer++;
    // console.log(timer);

    // console.log("The value of the opacity here is,", fasterOpacity);
    // console.log(typeof fasterOpacity);

    // console.log(typeof fasterOpacity);

    // fasterOpacity = fasterOpacity + 0.05;

    // console.log(fasterOpacity);

    // faster.style.opacity = fasterOpacity.toString();

    // console.log(fasterOpacity);